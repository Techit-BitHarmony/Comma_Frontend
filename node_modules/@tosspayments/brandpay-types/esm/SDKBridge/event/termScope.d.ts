import { TermScope } from '../../models';
import { BaseMessageData } from '../baseMessageData';
export declare const MessageName: "TERM_SCOPE";
export interface TermScopeMessageData extends BaseMessageData {
    type: typeof MessageName;
    result: Array<keyof TermScope>;
}
