import { BaseMessageData } from '../baseMessageData';
export declare const MessageName: "CANCEL";
interface CancelParams {
    code: string;
    message: string;
}
export interface CancelMessageData extends BaseMessageData {
    type: typeof MessageName;
    params?: CancelParams;
}
export {};
