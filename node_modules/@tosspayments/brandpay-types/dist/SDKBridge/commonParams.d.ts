/**
 * @description
 * 가맹점에서 브랜드페이를 호출할 때 넘겨주는 파라미터 및 가맹점/유저 식별을 위한 식별자 정보를 담고 있는 파라미터입니다
 */
export interface CommonParams {
    sessionId: string;
    clientId: string;
    clientKey: string;
    customerKey: string;
    origin: string;
    options?: BrandPayOptions;
}
/**
 * @description
 * 가맹점이 브랜드페이를 호출할 때 optional하게 넘겨주는 파라미터입니다.
 * @see https://www.notion.so/tossteam/UI-Javascript-5d71203e00f240dda7d7aa0b169e7b96
 */
export interface BrandPayOptions {
    redirectUrl?: string;
    ocrLicense?: string;
    /** @deprecated */
    showNavigationBar?: boolean;
    /** @deprecated */
    theme?: Theme;
    /** @deprecated */
    labels?: Labels;
    ui?: UiOptions;
    windowTarget?: 'iframe' | 'popup';
    version?: 'brandpay' | 'oliveyoung' | 'cafe24';
    /**
     * global trace id
     * @internal
     */
    gtid?: string;
    /**
     * 어느 서비스에서 SDK 를 로드했는지 나타냄
     * @internal
     */
    service?: 'sdk' | 'widget' | 'payment-widget' | 'unknown';
}
export declare type Theme = 'musinsa' | 'simsale' | 'golfzon' | 'giftistar';
interface UiOptions {
    /** @deprecated */
    showNavigationBar?: boolean;
    highlightColor?: string;
    buttonStyle?: 'default' | 'full';
    labels?: Labels;
    navigationBar?: NavigationBar;
    paymentMethodBottomSheet?: PaymentMethodBottomSheet;
}
interface Labels {
    oneTouchPay?: string;
}
interface NavigationBar {
    visible: boolean;
    paddingTop: number;
}
interface PaymentMethodBottomSheet {
    addMethod?: AddMethod;
}
interface AddMethod {
    visible: boolean;
}
export {};
