import { BaseMessageData } from '../../baseMessageData';
export declare const MessageName: "MOUNT";
export interface WidgetMountMessageData extends BaseMessageData {
    type: typeof MessageName;
    params: {
        mount: boolean;
    };
}
