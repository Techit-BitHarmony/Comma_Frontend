import { PaymentRequest, PaymentRequestOptions, PaymentRequestWithoutUrl } from './PaymentRequest';
export interface CreatePaymentResponse {
    amount: number;
    approvalNo?: string;
    createdAt?: string;
    currency: string;
    customerKey?: string;
    installmentPlan?: number;
    key: string;
    method?: {
        card?: {
            cardNumber: string;
            financeCode: number;
            financeName: string;
        };
        key: string;
        type: string;
        virtualAccount?: {
            accountHolder: string;
            accountNumber: string;
            bankCode: string;
            bankName: string;
            dueDate: string;
        };
    };
    orderId?: string;
    status: string;
    statusDetail: string;
}
export interface CreatePaymentOptions extends PaymentRequestOptions {
    isLegacy?: boolean;
    language?: 'en' | 'ko';
}
export default function createPayment(clientKey: string, { mId, ...params }: PaymentRequest | PaymentRequestWithoutUrl, options: CreatePaymentOptions): Promise<CreatePaymentResponse>;
